{
  "name": "using-cmux-zh",
  "version": "1.8.0",
  "description": "中文版 - cmux 终端操作技能包。面板分割、子代理启动/监控/结果回收、命令发送、屏幕读取、通知。",
  "author": {
    "name": "1dooooo (translated from hummer98)"
  },
  "license": "MIT",
  "repository": "https://github.com/1dooooo/using-cmux",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$CMUX_SURFACE_ID\" ] && [ -z \"$CMUX_NO_RENAME_TAB\" ]; then REF=$(cmux identify | jq -r '.caller.surface_ref'); NUM=$(echo \"$REF\" | cut -d: -f2); cmux rename-tab --surface \"$REF\" \"[$NUM] Claude Code\"; fi"
          }
        ]
      }
    ]
  }
}
